/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/login-01pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Login01preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Login 01pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
