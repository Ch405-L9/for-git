/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/logos-02pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Logos02preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Logos 02pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
