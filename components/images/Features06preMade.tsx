/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/features-06pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Features06preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Features 06pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
