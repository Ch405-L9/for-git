/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/logos-07pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Logos07preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Logos 07pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
