/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/testimonial-06.webp';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Testimonial06({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Testimonial 06'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
