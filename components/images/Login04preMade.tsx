/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/login-04pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Login04preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Login 04pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
