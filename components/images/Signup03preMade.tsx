/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/signup-03pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Signup03preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Signup 03pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
