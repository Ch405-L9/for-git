/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/footer-05pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Footer05preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Footer 05pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
