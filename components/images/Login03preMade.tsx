/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/login-03pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Login03preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Login 03pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
