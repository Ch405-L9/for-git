/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/login-05pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Login05preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Login 05pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
