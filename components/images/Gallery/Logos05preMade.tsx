/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/logos-05pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Logos05preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Logos 05pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
