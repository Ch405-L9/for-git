/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/footer-04pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Footer04preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Footer 04pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
