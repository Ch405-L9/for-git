/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/signup-04pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Signup04preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Signup 04pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
