/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/footer-02pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Footer02preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Footer 02pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
