/* auto-generated; do not edit */
import React from 'react';
import src from '../../assets/contact-01pre-made.png';
type Props = Omit<React.ImgHTMLAttributes<HTMLImageElement>, 'src' | 'alt'> & { alt?: string };
export default function Contact01preMade({ alt, ...rest }: Props) {
  return (
    <img
      src={String(src)}
      alt={alt ?? 'Contact 01pre Made'}
      loading="lazy"
      decoding="async"
      {...rest}
    />
  );
}
